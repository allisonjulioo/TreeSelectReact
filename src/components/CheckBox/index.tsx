import React, { PureComponent } from 'react';
import './styles.scss';

export default class extends PureComponent {
  render() {
    return <input type="checkbox" id="checkbox" className="checkbox-input" />;
  }
}
